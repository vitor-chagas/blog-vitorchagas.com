---
import '../styles/global.css';
import { ClientRouter } from 'astro:transitions';
import SocialLinks from '../components/SocialLinks.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Developer building at the intersection of AI and video.' } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const fullTitle = title === 'Vitor Chagas' ? title : `${title} — Vitor Chagas`;

const pathname = Astro.url.pathname;
const isHome = pathname === '/';
const isProjects = pathname.startsWith('/projects');
const isBlog = pathname.startsWith('/blog');

const navLink = (active: boolean) =>
  `text-xs font-semibold tracking-widest uppercase pb-1 border-b-2 transition-colors duration-150 ${
    active
      ? 'text-heading border-accent'
      : 'text-nav border-transparent hover:text-heading hover:border-border'
  }`;
---

<!doctype html>
<html lang="en" class="bg-page antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="canonical" href={canonicalURL} />
    <meta name="description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap"
      rel="stylesheet"
    />
    <title>{fullTitle}</title>
    <ClientRouter />
  </head>

  <body class="min-h-screen p-3 sm:p-6 lg:p-10 flex flex-col items-center text-body">
    <div class="w-full max-w-4xl bg-card border border-card-border rounded-2xl overflow-hidden flex flex-col min-h-[calc(100vh-6rem)]">

      <!-- Nav -->
      <header class="border-b border-border px-6 sm:px-8 py-4">
        <div class="flex items-center gap-4">

          <!-- Left: identity -->
          <a href="/" class="flex items-center gap-3 w-48 shrink-0 no-underline">
            <img
              src="/avatar.png"
              alt="Vitor Chagas"
              class="w-9 h-9 rounded-full object-cover shrink-0"
            />
            <div class="min-w-0">
              <div class="text-sm font-semibold text-heading leading-tight truncate">Vitor Chagas</div>
              <div class="text-xs text-muted leading-tight">Data & DevOps Engineer</div>
            </div>
          </a>

          <!-- Center: nav -->
          <nav class="flex-1 flex items-center justify-center gap-8">
            <a href="/" class={navLink(isHome)}>About Me</a>
            <a href="/projects" class={navLink(isProjects)}>Projects</a>
            <a href="/blog" class={navLink(isBlog)}>Writing</a>
          </nav>

          <!-- Right: social icons -->
          <div class="flex items-center gap-3.5 w-48 shrink-0 justify-end text-muted">
            <SocialLinks />
          </div>

        </div>
      </header>

      <!-- Page content -->
      <main class="flex-1">
        <slot />
      </main>

      <!-- Footer -->
      <footer class="border-t border-border px-6 sm:px-8 py-5 flex items-center justify-between">
        <span class="text-xs text-muted">© {new Date().getFullYear()} Vitor Chagas</span>
        <div class="flex items-center gap-3.5 text-muted">
          <SocialLinks />
        </div>
      </footer>

    </div>
  </body>
</html>
